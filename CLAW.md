# CLAW

Alias runtime contract for teams using `CLAW.md` naming.

This file intentionally mirrors the practical intent of `CLAUDE.md`:

- follow `AGENTS.md` first
- separate chronology (`wiki/log.md`) from synthesis (`wiki/*.md`)
- keep wiki pages source-grounded
- preserve lightweight, tool-agnostic markdown
- favor explicit uncertainty over fabricated certainty
- keep public templates free of private data

Primary detailed guidance remains in `CLAUDE.md`.
